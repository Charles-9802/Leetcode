max(dic, key=lambda x: dic[x])
max(dic, key=dic.get)

# 扩展为一维数组
# out = []
# def swy(seq):
#     for item in seq:
#         if isinstance(item, list):
#             swy(item)
#         else:
#             out.append(item)
#     return out
#
#
# print(swy([1, [2, 3], [[4, 5], [6]]]))

# 去掉字符串前空格
# length = len(str)
# while str[i] == " ":
#     i += 1
#     if i == length:
#         return 0